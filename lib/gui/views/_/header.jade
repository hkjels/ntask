header
  - if (typeof task !== 'undefined')
    h1= task.labels[0]
    a(href='/').back &larr; Back
  - else if (typeof tasks !== 'undefined')
    h1= meta.name
    form#search
      fieldset
        input(type='text', placeholder='Filter tasks', autofocus='autofocus')